<% if @posts.any? %>
	<% @posts.each do |post| %>
		<h2><%= post.title %></h2>
		<%= image_tag post.image.url(:thumb) %>
	<% end %>
	<% else %>
	No posts yet!
	<% end %>


<a href='posts/new'>Add a post</a>